import { ContentHeightDirective } from './content-height.directive';

describe('ContentHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentHeightDirective();
    expect(directive).toBeTruthy();
  });
});
